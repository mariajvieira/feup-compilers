class ComplexArithmetic {
    public int foo() {
        int a;
        int b;
        int c;
        a = 2 + 3 * 4 - 5 / (1 + 1) + 6 * (7 - 2);
        b = (2 + 3) * (4 - 5) / 1 + 10 - 3 * 2;
        c = a - b + (a + b) * (2 - 3) / (4 + 1) - 1 + 3 / (4 - 1);
        return c;
    }
}